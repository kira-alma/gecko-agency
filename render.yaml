services:
  - type: web
    name: geckocheck
    runtime: docker
    plan: starter
    disk:
      name: geckocheck-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
